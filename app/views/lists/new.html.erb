<div class="login-form">
  <h1>Create a new list</h1>

  <%= form_for List.new do |f| %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
  <% end %>

</div>